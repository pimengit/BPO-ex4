<Window x:Class="BPO_ex4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BPO_ex4" 
        Title="Logic Studio" Height="700" Width="1200">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <Border Background="#F3F3F3" Padding="10" BorderBrush="#CCC" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Margin" Value="2"/>
                        </Style>
                    </Grid.Resources>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="Загрузить конфиг" Click="BtnLoad_Click" Grid.ColumnSpan="2" Margin="2,2,38,2"/>
                    <Button Grid.Row="0" Grid.Column="1" Content="Сохранить конфиг" Click="BtnSave_Click" Grid.ColumnSpan="2" Margin="75,2,2,2"/>

                    <Button Grid.Row="1" Grid.Column="0" Content="Лог" Width="50" Click="BtnOpenLog_Click"/>
                    <Button Grid.Row="1" Grid.Column="1" Content="Станция" Click="BtnOpenScheme_Click"/>
                    <Button Grid.Row="1" Grid.Column="2" Content="Пересчитать" Click="BtnRecompute_Click" 
                Background="#444" Foreground="White" FontWeight="Bold"/>
                </Grid>
            </Border>

            <TextBox Name="TxtSearchType" Grid.Row="1" Margin="0,5" Padding="3" TextChanged="TxtSearchType_TextChanged" Text="Поиск типа..."/>
            <ListBox Name="LstTypes" Grid.Row="2" SelectionChanged="LstTypes_SelectionChanged"/>

            <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch"/>

            <ListBox Name="LstInstances" Grid.Row="4" SelectionChanged="LstInstances_SelectionChanged" BorderBrush="Gray">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Id}" FontWeight="Bold" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Description}" Foreground="Gray" FontStyle="Italic" TextTrimming="CharacterEllipsis" MaxWidth="150"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#DDD"/>

        <TabControl x:Name="MainTabs" Grid.Column="2" Margin="5"/>

    </Grid>
</Window>