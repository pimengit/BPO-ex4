<Window x:Class="BPO_ex4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BPO Logic Editor" Height="600" Width="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <Button Name="BtnLoad" Content="Load Excel Project" Padding="10,5" Click="BtnLoad_Click" Background="#DDDDDD"/>
            <TextBox Name="TxtSearchType" Grid.Row="1" Margin="0,5" Padding="5" TextChanged="TxtSearchType_TextChanged"/>
            <ListBox Name="LstTypes" Grid.Row="2" SelectionChanged="LstTypes_SelectionChanged" BorderBrush="Gray"/>
            <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch" Background="LightGray"/>

            <ListBox Name="LstInstances" Grid.Row="4" SelectionChanged="LstInstances_SelectionChanged" BorderBrush="Gray">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Id}" FontWeight="SemiBold" Foreground="Black"/>
                            <TextBlock Text="{Binding Description}" Margin="10,0,0,0" Foreground="Gray" FontStyle="Italic"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#CCCCCC"/>

        <DockPanel Name="GraphContainer" Grid.Column="2" Background="White" ClipToBounds="True"/>
    </Grid>
</Window>